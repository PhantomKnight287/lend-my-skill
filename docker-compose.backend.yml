services:
  backend:
    container_name: lend-my-skill-backend
    env_file:
      - apps/backend/.env
    build:
      context: apps/backend
      dockerfile: Dockerfile
      tags:
        - "phantomknight/lms-backend:latest"
    networks:
      - lend-my-skill
    ports:
      - 5000:5000
    
    
  

networks:
  lend-my-skill:
